generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Menu {
  id          String     @id @default(cuid())
  title       String
  sundayDate  DateTime
  cutoffAt    DateTime
  pickupNotes String     @default("Pickup Sunday, 5-6pm at Grey Goose Players Club.")
  status      MenuStatus @default(DRAFT)
  soldOut     Boolean    @default(false)
  createdAt   DateTime   @default(now())
  updatedAt   DateTime   @updatedAt
  items       MenuItem[]
  orders      Order[]
}

model MenuItem {
  id          String   @id @default(cuid())
  menuId      String
  menu        Menu     @relation(fields: [menuId], references: [id], onDelete: Cascade)
  sortOrder   Int
  name        String
  description String
  imageUrl    String?
}

model Order {
  id                    String      @id @default(cuid())
  menuId                String
  menu                  Menu        @relation(fields: [menuId], references: [id])
  customerName          String
  customerEmail         String
  customerPhone         String
  notes                 String?
  allergens             String?
  portionId             String
  portionName           String
  quantity              Int
  subtotalCents         Int
  tipCents              Int         @default(0)
  processingFeeCents    Int         @default(0)
  totalCents            Int
  stripeSessionId       String?     @unique
  stripePaymentIntentId String?
  status                OrderStatus @default(PENDING)
  createdAt             DateTime    @default(now())
  updatedAt             DateTime    @updatedAt
}

model SiteImage {
  id        String   @id @default(cuid())
  label     String
  url       String
  alt       String?
  createdAt DateTime @default(now())
}

model Setting {
  key       String   @id
  value     String
  updatedAt DateTime @updatedAt
}

model CateringInquiry {
  id           String        @id @default(cuid())
  customerName String
  email        String
  phone        String
  eventDate    DateTime?
  eventTime    String?
  guestCount   Int?
  budget       String?
  eventType    String?
  location     String?
  notes        String?
  status       InquiryStatus @default(NEW)
  createdAt    DateTime      @default(now())
  updatedAt    DateTime      @updatedAt
}

enum MenuStatus {
  DRAFT
  PUBLISHED
  ARCHIVED
}

enum OrderStatus {
  PENDING
  PAID
  CANCELED
  REFUNDED
}

enum InquiryStatus {
  NEW
  CONTACTED
  BOOKED
  DECLINED
}
